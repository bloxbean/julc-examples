package com.example.benchmark.wingriders;

import com.bloxbean.cardano.julc.stdlib.annotation.OnchainLibrary;

/**
 * Shared constants for WingRiders benchmark.
 * <p>
 * Note: All datum/redeemer types must be defined inside validator classes
 * for the compiler to discover them. This file holds only shared helpers.
 */
@OnchainLibrary
public class WingRidersTypes {
    // Marker class for library discovery - actual types are in each validator
}
